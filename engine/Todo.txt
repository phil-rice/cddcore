
An engine description. For the website
Macros: The because/code/withDefaultCode should become macros on engine. That way I can get rid of the type signatures in the because 
Consider: The expected might also be able to take a parameter list, so that the input can be copied to the output...
CSS for the improved website


Special Engines
* Ranges (dates / numbers / amounts). We know it's about partitioning the range, so let's make it easy
* XML We can have special because clauses that allow you to specify one or more fragments, then make statements about the fragments. this allows us to highlight the code better in the documentation and error messages

